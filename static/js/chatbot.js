const form=$("#chatForm"),input=$("#chatMessage"),conversation=$("#conversation");
on(form,"submit",async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;conversation.insertAdjacentHTML("beforeend",`<article class="message user"><strong>You</strong><p>${escapeHTML(text)}</p></article>`);input.value="";const res=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text})});const json=await res.json();conversation.insertAdjacentHTML("beforeend",`<article class="message bot"><strong>FinWise AI</strong><p>${escapeHTML(json.reply||json.error||"Unable to reply.")}</p></article>`);conversation.scrollTop=conversation.scrollHeight});
$$(".prompt-panel button").forEach(b=>on(b,"click",()=>{input.value=b.textContent;input.focus()}));
